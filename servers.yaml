- server:
      port: 443
      domain:
        subject: stepca.local
      https: /var/local/step
      log: /var/log/nginx
      locations:
          - type: proxy_pass
            value: http://ca:5739
            include: /etc/nginx/includes/default.proxy.conf
      redirect: 80

